# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Data validation & settings ────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Database (SQLite for MVP, SQLAlchemy abstracts the driver) ─────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# ── HTTP client (used by all real external API providers) ─────────────────────
httpx>=0.27.0

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── LLM providers (optional — mock mode works without these) ──────────────────
openai>=1.0.0
anthropic>=0.40.0

# ── Maps / Places SDK (optional — mock mode uses Haversine + local JSON) ──────
googlemaps>=4.10.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
